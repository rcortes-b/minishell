#include "../../includes/builtins.h"

/* Ruta Absoluta: cd /Users/rcortes-/Common_Core/working_minishell/directory 
   Ruta Relativa: cd directory */
/* Como saber si absoluta o relativa??? en 'env' hay un apartado llamado home 
Puedes hacer ruta absoluta desde /Users/rcortes-/home y desde ~/, cual es el bueno? */

static int	path_is_relative(char *path)
{

}

void	change_directory(t_word *word)
{
	int	i;

	i = 1;
}